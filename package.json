{
  "name": "enterprise-procurement-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Enterprise procurement and system integration platform",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "watch:cap": "npm --prefix apps/cap exec -- cds watch",
    "test": "echo \"No tests in Stage 2\" && exit 0"
  }
}